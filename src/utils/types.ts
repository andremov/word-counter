type LocaleStrings = {
  title: string;
  labels: {
    data: string;
    input: string;
    chars: string;
    words: string;
  };
  buttons: {
    copy: string;
    copied: string;
  };
};

export default LocaleStrings;
